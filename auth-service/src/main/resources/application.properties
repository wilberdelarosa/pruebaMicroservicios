spring.application.name=auth-service
server.port=8081

# Railway MySQL external connection
spring.datasource.url=${DB_URL:jdbc:mysql://yamanote.proxy.rlwy.net:13316/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:zjLijrOvmRXeMsJxUxRSTfEzFFadJGFS}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=${JWT_SECRET:my-super-secret-key-at-least-64-characters-long-for-hs512-algorithm-security}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging
logging.level.root=INFO
logging.level.com.eventos=DEBUG
